-- cue.mod/module.cue --
module: "example.com"

-- one.cue --
package test

Out: {
    Attrib
    NoAttrib
    baz: string
} @cuetsy(kind="interface")

Attrib: {
    Nested: {
      inner: string
    }
    foo: string
} @cuetsy(kind="interface")

NoAttrib: {
    Nested: {
      inner: int
    }
    bar: string
}

-- out/gen --
Input value must be valid